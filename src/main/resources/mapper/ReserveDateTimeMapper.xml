<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="ReserveDate">
    <select id="findAll" resultType="reserveDate">
        select * from reservation_table where reserveDate >= DATE_FORMAT(now(),'%Y-%m-%d') order by reserveDate asc;
    </select>
    <insert id="save" parameterType="reserveDate">
        insert into reservation_table(reserveDate)
        values (#{reserveDate})
    </insert>
    <select id="findByDate" parameterType="java.time.LocalDate" resultType="reserveDate">
        select * from reservation_table where reserveDate=#{reserveDate}
    </select>
    <update id="update1" parameterType="reserveDate">
        update reservation_table set dT1=#{dT1} where reserveDate=#{reserveDate}
    </update>
    <update id="update2" parameterType="reserveDate">
        update reservation_table set dT2=#{dT2} where reserveDate=#{reserveDate}
    </update>
    <update id="update3" parameterType="reserveDate">
        update reservation_table set dT3=#{dT3} where reserveDate=#{reserveDate}
    </update>
    <update id="update4" parameterType="reserveDate">
        update reservation_table set dT4=#{dT4} where reserveDate=#{reserveDate}
    </update>
    <update id="update5" parameterType="reserveDate">
        update reservation_table set dT5=#{dT5} where reserveDate=#{reserveDate}
    </update>
    <update id="update6" parameterType="reserveDate">
        update reservation_table set dT6=#{dT6} where reserveDate=#{reserveDate}
    </update>
</mapper>